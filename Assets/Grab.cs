using System;
using UnityEngine;
using System.Collections;
using System.Collections.Generic;
using Kinect = Windows.Kinect;

public class Grab : HandTracker {

	public Vector3 position;

	// Use this for initialization
	/*void Start () 
	{
		bool grabbed = false;
	}
	
	// Update is called once per frame
	void Update () 
	{
//		Kinect.JointType hand = Kinect.JointType.HandRight
		position = GetVector3FromJoint (Kinect.);

		print ("pos: " + position);

		//if hand was open when it came in the same XY coordiantes and is now closed
	}*/

} 